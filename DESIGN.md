# Design System

## Source of Truth

本文件供自动化设计工具读取。项目的完整视觉与交互规范以 `docs/design-system/` 下的当前有效文档为唯一详细基线，尤其是《视觉设计规范.md》《UI与交互规范.md》《信息架构与页面结构.md》和《用户旅程.md》。发生冲突时，以这些中文规范和用户最新确认的方向为准。

## Theme

纯白实体阅读画布、纯黑焦点与主操作、由黑色透明度形成的灰阶。辅助色只用于完成、提醒、错误、引用关系和 Agent 身份等必要语义，整体占比不超过 5%。

## Typography

使用系统字体栈，中文优先 PingFang SC 与 HarmonyOS Sans SC。正文 16px，行高 1.6 至 1.75；页面 H1 为 30 至 36px；阶段标题克制，依靠字号、字重、留白和对齐建立编辑式层级。

## Layout

移动端优先，基础间距单位 4px，常用序列为 8、12、16、24、32、48、64px。页面左右安全边距通常为 20px。阅读页使用稳定单列和舒适行长，不使用卡片网格，不以大口号占据首屏。

## Materials

内容层保持平面实体；导航、筛选和短时控制使用轻薄 Liquid Glass；Agent 抽屉使用更厚玻璃；主按钮和确认操作使用纯黑或纯白实体。玻璃不得成为正文容器或装饰背景。

## Components

图标统一为单色 SVG；触控目标至少 44px；主要按钮使用实体黑白表面；普通列表通过留白、对齐和细分隔线组织；圆角主要属于控制和浮层，正文与普通信息区默认无圆角。

## Motion

普通内容和控件以 150 至 300ms 的克制位移、透明度和吸附反馈表达状态。地图和抽屉允许更明显的阻力与惯性。禁止装饰性连续运动；系统开启减少动态效果时取消大范围移动、视差和弹簧回弹。
