# 管理后台环境变量
# 复制此文件为 .env（.env 已加入 .gitignore，不会被提交）
#
# 本地开发时通常不需要改这个文件——
# Vite 开发服务器会自动把 /api/* 请求代理到 localhost:3456
# 只有需要指向其他 LLM 代理地址时才修改

# LLM 代理服务地址（默认通过 Vite proxy 转发，无需设置）
# VITE_API_BASE_URL=http://localhost:3456
